{
  "adminToken": "some secure token for authorizing with /admin/ routes",
  "host": "the host for the api, for returning well formed urls",
  "client": {
    "adminId": "Discord user id for the service admin, for the /upload command",
    "key": "Application public key for verifying incoming interactions"
  }
}
